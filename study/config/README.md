# `study/config/` — Evaluation thresholds

Parent: [`../README.md`](../README.md).

---

## Files

| File | Purpose |
|---|---|
| `study_config.yaml` | Gate thresholds for script `09_research_evaluation.py` (Jaccard caps, judge score floor, within/between cluster bounds, contrast pairs, etc.) |

Treat these as **pre-registered** research settings. Do not silently loosen gates to force a pass — fix the prompt/generator instead (see study README gate policy).
